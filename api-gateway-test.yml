server:
  port: 9000

#自定义路由映射
zuul:
  routes:
    order-service: /apigateway/order/**
    product-service: /apigateway/product/**
  ignored-patterns: /*-service/**
  #处理HTTP请求头为空的问题
  sensitive-headers:
